import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.dashboard.DashboardWidgetLayout'

export default defineMessages({
    widgetTaskSummary: {
        id: `${scope}.Widget.TaskSummary.title`,  
        defaultMessage: 'Tasks Summary',
    },    
})