import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.dashboard.DashboardAddWidget'

export default defineMessages({
    addNewTitle: {
        id: `${scope}.addNewWidget`,  
        defaultMessage: 'Add widget',
    },
  })