import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.dashboard.DashboardMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Dashboard',
    }
})