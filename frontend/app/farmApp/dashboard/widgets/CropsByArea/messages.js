import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.dashboard.widgets.CropsByArea'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Crops by area',
    },    
})