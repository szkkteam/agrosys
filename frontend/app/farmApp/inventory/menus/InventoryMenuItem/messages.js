import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.inventory.InventoryMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Inventory',
    }
})