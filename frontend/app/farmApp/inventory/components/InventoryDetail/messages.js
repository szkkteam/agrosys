import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.inventory.InventoryDetail'

export default defineMessages({
    title: {
        id: `${scope}.Workers.title`,  
        defaultMessage: 'Workers',
    },    
  })