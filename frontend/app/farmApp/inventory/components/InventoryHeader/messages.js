import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.inventory.InventoryHeader'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Inventory',
    }
  })