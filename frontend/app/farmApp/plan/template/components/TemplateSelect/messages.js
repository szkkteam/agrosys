import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.plan.template.TemplateSelect'

export default defineMessages({
    template: {
        id: `${scope}.field.template`,  
        defaultMessage: "Select a task template",
    }
  })
