import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.plan.cropPlan.CropPlanSelect'

export default defineMessages({
    cropType: {
        id: `${scope}.field.cropPlan`,  
        defaultMessage: "Crop plan",
    }
  })
