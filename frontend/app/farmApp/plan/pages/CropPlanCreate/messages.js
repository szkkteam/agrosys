import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.plan.CropPlanCreate'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Create new crop plan',
    },
  })