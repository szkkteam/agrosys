import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.plan.PlanList'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Plans',
    },
  })