import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.plan.fieldPlan.FieldPlanNewPlanButton'

export default defineMessages({
    
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'New field plan',
    },
  })