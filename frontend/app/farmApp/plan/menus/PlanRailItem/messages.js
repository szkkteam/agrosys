import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.plan.PlanRailItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Plan',
    }
})