import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.plan.PlanMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Season Planner',
    }
})