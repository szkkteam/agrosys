import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.block.BlockMapView.BlockList'

export default defineMessages({
    listTitle: {
        id: `${scope}.title`,  
        defaultMessage: 'Fields list',
    },
  })