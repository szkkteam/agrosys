import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.block.BlockCreateLPIS'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Select field',
    },
  })