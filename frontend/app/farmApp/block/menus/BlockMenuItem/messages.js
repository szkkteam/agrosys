import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.block.BlockMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Fields',
    }
})