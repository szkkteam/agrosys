import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.Tutorial'

export default defineMessages({
    tutorialProgress: {
      id: `${scope}.TutorialProgressBar.progress`,
      defaultMessage: 'Progress',
    },
  })