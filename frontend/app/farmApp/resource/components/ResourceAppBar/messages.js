import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.resource.ResourceAppBar'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Resource',
    },    
})