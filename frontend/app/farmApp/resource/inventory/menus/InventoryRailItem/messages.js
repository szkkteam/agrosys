import { defineMessages } from 'react-intl'

export const scope = 'app.resource.resource.InventoryRailItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Inventory',
    }
})