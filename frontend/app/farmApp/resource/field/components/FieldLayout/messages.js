import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.field.FieldLayout'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Agricultural Parcels',
    },
  })