import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.resource.field.FieldDrawDialog'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Draw a field on the map',
    },    
  })