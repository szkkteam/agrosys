import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.resource.field.FieldCreateGridView'

export default defineMessages({
    drawNew: {
        id: `${scope}.DrawNew.title`,  
        defaultMessage: 'Draw field',
    }
  })