import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.resource.field.FieldMapAppBar'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Worker - database',
    },    
})