import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.resource.field.FieldMap'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: "Field XXX - Map",
    },
  })