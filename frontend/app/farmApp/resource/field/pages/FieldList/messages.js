import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.resource.field.FieldList'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Parcels',
    },
  })