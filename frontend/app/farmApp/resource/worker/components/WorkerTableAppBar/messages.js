import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.resource.worker.WorkerTableAppBar'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Worker - database',
    },    
})