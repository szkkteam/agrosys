import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.resource.worker.WorkerRole'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Worker - roles',
    },
  })