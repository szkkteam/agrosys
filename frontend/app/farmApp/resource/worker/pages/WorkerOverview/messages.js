import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.resource.worker.WorkerOverview'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Worker',
    },
  })