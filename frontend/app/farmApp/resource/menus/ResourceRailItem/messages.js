import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.resource.ResourceRailItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Resource',
    }
})