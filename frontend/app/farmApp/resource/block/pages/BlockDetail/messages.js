import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.block.BlockDetail'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Fields - Detail',
    },
  })