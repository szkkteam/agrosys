import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.block.BlockList'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Fields',
    },
  })