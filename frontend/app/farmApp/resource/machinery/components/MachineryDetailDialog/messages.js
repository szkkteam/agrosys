import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.machinery.MachineryDetailDialog'

export default defineMessages({
    title: {
        id: `${scope}.Tab.title`,  
        defaultMessage: 'Machinery - Item',        
    },
    tabGeneral: {
        id: `${scope}.Tab.General`,  
        defaultMessage: 'General',        
    },
  })
