import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.resource.machinery.MachineryReservation'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Machinery - reservation',
    },
  })