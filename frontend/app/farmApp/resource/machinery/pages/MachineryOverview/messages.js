import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.resource.machinery.MachineryOverview'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Machinery',
    },
  })