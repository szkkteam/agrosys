import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.farm.FarmDashboard'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Dashboard',
    },
  })