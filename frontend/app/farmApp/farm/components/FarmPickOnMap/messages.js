import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.farm.FarmPickOnMap'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Pick on map',
    },
  })