/*
import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.farm.FarmHeader'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Create a new farm',
    },
  })
*/