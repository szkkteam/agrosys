import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.workers.WorkersMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Workers',
    }
})