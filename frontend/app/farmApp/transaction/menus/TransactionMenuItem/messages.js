import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.transaction.TransactionMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Transactions',
    }
})