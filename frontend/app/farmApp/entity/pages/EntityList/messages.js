import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.entity.EntityList'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Entities',
    },
  })