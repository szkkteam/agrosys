import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.sale.SaleDashboard'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Sales',
    },
  })