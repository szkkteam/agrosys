import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.operation.OperationTreatmentDialog'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Add treatment',        
    },
    
  })
