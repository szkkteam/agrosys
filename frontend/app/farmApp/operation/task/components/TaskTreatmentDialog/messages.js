import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.operation.TaskTreatmentDialog'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Add treatment',        
    },
    
  })
