import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.operation.OperationRailItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Operation',
    }
})