import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.OperationCreate'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'OperationCreate',
    },
  })