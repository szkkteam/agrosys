import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.operation.OperationMapOverview'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Tasks',
    },
  })