import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.OrderOverview'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'My tasks',
    },
  })