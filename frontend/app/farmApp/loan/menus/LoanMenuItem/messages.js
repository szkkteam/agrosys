import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.loan.LoanMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Loan',
    }
})