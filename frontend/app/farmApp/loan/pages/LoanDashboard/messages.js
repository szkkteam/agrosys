import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.loan.LoanDashboard'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Loans',
    },
  })