import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.traceability.TraceabilityMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Traceability',
    }
})