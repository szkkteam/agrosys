import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.notification.NotificationButton'

export default defineMessages({
    title: {
      id: `${scope}.title`,
      defaultMessage: 'Notifications',
    },
  })