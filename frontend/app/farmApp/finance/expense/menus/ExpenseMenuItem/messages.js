import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.expense.ExpenseMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Expenses',
    }
})