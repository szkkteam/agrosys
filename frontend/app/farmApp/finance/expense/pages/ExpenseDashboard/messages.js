import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.expense.ExpenseDashboard'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Expenses',
    },
  })