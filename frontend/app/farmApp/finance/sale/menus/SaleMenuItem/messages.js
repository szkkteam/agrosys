import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.sale.SaleMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Sales',
    }
})