import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.storage.StorageMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Storage',
    }
})