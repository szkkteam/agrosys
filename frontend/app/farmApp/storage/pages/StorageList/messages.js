import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.storage.StorageList'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Storages',
    },
  })