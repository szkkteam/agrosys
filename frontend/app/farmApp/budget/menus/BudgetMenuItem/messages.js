import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.budget.BudgetMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Budget',
    }
})