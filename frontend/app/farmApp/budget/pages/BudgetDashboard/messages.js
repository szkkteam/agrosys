import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.budget.BudgetDashboard'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Budgets',
    },
  })