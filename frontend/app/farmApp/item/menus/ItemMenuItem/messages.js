import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.item.ItemPlannerMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Register Items',
    }
})