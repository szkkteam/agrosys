import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.item.ItemList'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Items',
    },
  })