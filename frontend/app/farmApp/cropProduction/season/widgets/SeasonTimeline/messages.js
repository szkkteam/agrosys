import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.season.SeasonTimeline'

export default defineMessages({
    tooltip: {
        id: `${scope}.tooltip`,  
        defaultMessage: 'Manage and add season for your crop production',
    }, 
  })