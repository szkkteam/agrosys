import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.season.notifications'

export default defineMessages({
    seasonCreated: {
        id: `${scope}.Season.created`,  
        defaultMessage: "<b>my season</b>, created.",        
    },    
  })
