import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.season.SeasonSelector'

export default defineMessages({
    seasonTemplate: {
        id: `${scope}.field.seasonTemplate`,  
        defaultMessage: "Season template",
    }
  })
