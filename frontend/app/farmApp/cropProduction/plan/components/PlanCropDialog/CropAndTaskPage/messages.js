import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.plan.PlanCropDialog.CropAndTaskPage'

export default defineMessages({
    mainCropTitle: {
        id: `${scope}.mainCrop.title`,  
        defaultMessage: 'Main crop - {crop}',
    },   
    
  })