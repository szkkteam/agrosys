import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.plan.PlanSelect'

export default defineMessages({
    seasonTemplate: {
        id: `${scope}.field.seasonTemplate`,  
        defaultMessage: "Season template",
    }
  })
