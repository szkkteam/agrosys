import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.plan.PlanLayout'

export default defineMessages({
    step1: {
        id: `${scope}.step1.title`,  
        defaultMessage: 'Main crop',
    },
    step2: {
        id: `${scope}.step2.title`,  
        defaultMessage: 'Secondary crop',
    },
    
  })