import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.plan.PlanAppBar'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Create a new plan',
    },    
})