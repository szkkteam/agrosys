import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.plan.PlanCreate'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Create plan',
    },
  })