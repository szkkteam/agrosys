import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.crop.notifications'

export default defineMessages({
    userCropCreated: {
        id: `${scope}.UserCrop.created`,  
        defaultMessage: "<b>my crop</b>, created.",        
    },    
  })
