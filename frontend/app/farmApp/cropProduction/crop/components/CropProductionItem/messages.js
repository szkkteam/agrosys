import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.crop.CropProductionItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Crops',
    },    
  })