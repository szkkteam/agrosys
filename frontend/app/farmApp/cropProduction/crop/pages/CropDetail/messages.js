import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.crop.CropDetail'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: "Crop - {crop}",
    },
  })