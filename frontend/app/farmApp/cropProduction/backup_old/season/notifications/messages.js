import { defineMessages } from 'react-intl'

export const scope = 'DELETE'

export default defineMessages({
    seasonCreated: {
        id: `${scope}.Season.created`,  
        defaultMessage: "<b>my season</b>, created.",        
    },    
  })
