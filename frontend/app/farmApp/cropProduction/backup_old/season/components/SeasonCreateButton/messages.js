import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.season.SeasonCreateButton'

export default defineMessages({
    addNewTitle: {
        id: `${scope}.AddButton.title`,  
        defaultMessage: 'Add new season',
    }, 
  })