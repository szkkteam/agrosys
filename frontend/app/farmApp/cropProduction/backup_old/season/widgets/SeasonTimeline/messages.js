import { defineMessages } from 'react-intl'

export const scope = 'DELETE'

export default defineMessages({
    tooltip: {
        id: `${scope}.tooltip`,  
        defaultMessage: 'Manage and add season for your crop production',
    }, 
  })