import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.crop.CropSettings'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Crops XXX - Settings',
    },
  })