import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.task.TaskOverview'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Tasks',
    },
  })