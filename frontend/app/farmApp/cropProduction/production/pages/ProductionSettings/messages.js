import { defineMessages } from 'react-intl'
import { messagesScope } from '../../constants'

const scope = 'app.farmApp.cropProduction.production.ProductionSettings'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Production XXX - Settings',
    },
  })