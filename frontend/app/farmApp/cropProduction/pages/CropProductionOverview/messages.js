import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.CropProductionOverview'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Crops',
    },
  })