import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.ProductionRailItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Crop',
    }
})