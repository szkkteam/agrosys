import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.cropProduction.CropRailItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Crop',
    }
})