import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.report.ReportMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Reports',
    }
})