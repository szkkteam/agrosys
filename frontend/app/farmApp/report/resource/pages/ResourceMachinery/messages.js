import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.report.resource.ResourceMachinery'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Machineries',
    },
  })