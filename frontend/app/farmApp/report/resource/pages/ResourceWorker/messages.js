import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.report.resource.ResourceWorker'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Workers',
    },
  })