import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.report.resource.ResourceUsage'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Usage',
    },
  })