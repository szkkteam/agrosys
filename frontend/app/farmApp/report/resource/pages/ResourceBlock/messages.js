import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.report.resource.ResourceBlock'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Fields',
    },
  })