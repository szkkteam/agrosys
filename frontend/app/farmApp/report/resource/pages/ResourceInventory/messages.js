import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.report.resource.ResourceInventory'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Inventory',
    },
  })