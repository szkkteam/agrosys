import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.report.ReportDashboard'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Reports',
    },
  })