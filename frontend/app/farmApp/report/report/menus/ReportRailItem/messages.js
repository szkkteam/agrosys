import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.report.report.ReportRailItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Report',
    }
})