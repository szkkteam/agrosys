import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.report.country.CountryTabHeader'

export default defineMessages({
    tabSummaryTitle: {
        id: `${scope}.Summary.title`,  
        defaultMessage: 'Summary',
    },
    tabGnTitle: {
        id: `${scope}.GN.title`,  
        defaultMessage: 'Gazdasági Napló',
    },
   
  })