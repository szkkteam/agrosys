import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.report.country.CountryGn'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Gazdasági Napló',
    },
  })