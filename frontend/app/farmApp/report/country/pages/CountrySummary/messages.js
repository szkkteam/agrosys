import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.report.country.CountrySummary'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Country reports',
    },
  })