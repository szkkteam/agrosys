import { defineMessages } from 'react-intl'

const scope = 'app.farmApp.production.ProductionTopSelector'

export default defineMessages({
    multiview: {
      id: `${scope}.multiView`,
      defaultMessage: 'Show all production',
    },
    create: {
        id: `${scope}.createNew`,
        defaultMessage: 'Create New',
      },
  })