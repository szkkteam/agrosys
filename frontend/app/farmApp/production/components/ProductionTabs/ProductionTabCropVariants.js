import React from 'react'
import messages from './messages';
import PropTypes from 'prop-types'
import { FormattedMessage } from 'react-intl';

const ProductionTabCropVariants = ({
    ...props
}) => {
    return (
        <div>Crop and Variants</div>
    )
}

ProductionTabCropVariants.propTypes = {

}

export default ProductionTabCropVariants
