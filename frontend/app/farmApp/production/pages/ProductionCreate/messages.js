import { defineMessages } from 'react-intl'
import { messagesScope } from '../../constants'

const scope = 'app.farmApp.production.ProductionCreate'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Create production',
    },
  })