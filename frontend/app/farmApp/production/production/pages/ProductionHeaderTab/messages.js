import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.resource.ResourceHeaderTab'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Crop productions',
    },
  })