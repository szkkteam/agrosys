import { defineMessages } from 'react-intl'

export const scope = 'app.resource.field.FieldList'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Parcels',
    },
  })