import { defineMessages } from 'react-intl'

export const scope = 'app.production.task.TaskList'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Tasks',
    },
  })