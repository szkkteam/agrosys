import { defineMessages } from 'react-intl'

export const scope = 'app.production.crop.CropOverview'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Crops',
    },
  })