import { defineMessages } from 'react-intl'

export const scope = 'app.site.LanguageSelector'

export default defineMessages({
    default: {
        id: `${scope}.default`,  
        defaultMessage: 'Select a language',
    },
  })