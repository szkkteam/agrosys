/*
import React, { useEffect, useMemo } from 'react'

import {
  ROUTE_MAP as farmAppRoutes,
  ROUTES as farmAppRoutesKeys,
} from 'farmApp/routes'

import {
  ROUTE_MAP as securityRoutes,
  ROUTES as securityRoutesKeys,
} from 'security/routes'

import {
  NotFound,
} from 'site/pages'

import { AnonymousRoute, ProtectedRoute } from 'utils/route'

export const ROUTES = {
  ...farmAppRoutesKeys,
  ...securityRoutesKeys,
}

export const ROUTE_MAP = {
  ...farmAppRoutes,
  ...securityRoutes,
}

console.debug("ROUTE_MAP: ", ROUTE_MAP)
*/