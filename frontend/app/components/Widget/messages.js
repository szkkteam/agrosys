import { defineMessages } from 'react-intl'

export const scope = 'app.components.Widget.WidgetContainer'

export default defineMessages({
    remove: {
        id: `${scope}.remove`,  
        defaultMessage: 'Remove',
    },    
})