import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.entities.EntitiesMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Entities',
    }
})