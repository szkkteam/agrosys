import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.farm.FarmSelector'

export default defineMessages({
    multifarm: {
      id: `${scope}.Multifarm`,
      defaultMessage: 'Multifarm View',
    },
    newFarm: {
        id: `${scope}.CreateNew`,
        defaultMessage: 'Create New',
      },
  })