import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.machinery.MachineryMenuItem'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Machinery',
    }
})