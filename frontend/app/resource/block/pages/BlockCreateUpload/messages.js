import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.block.BlockCreateUpload'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Upload field',
    },
  })