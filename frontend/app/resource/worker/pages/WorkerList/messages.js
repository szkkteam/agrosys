import { defineMessages } from 'react-intl'

export const scope = 'app.farmApp.worker.WorkerList'

export default defineMessages({
    title: {
        id: `${scope}.title`,  
        defaultMessage: 'Workers',
    },
  })